package com.modernbank.credit.context.propostas.domain.model;

/** Enum que representa o status da Proposta. */
public enum PropostaStatus {
  PENDENTE,
  PENDENTE_REVISAO,
  SUBMETIDA,
  APROVADA,
  REJEITADA
}
