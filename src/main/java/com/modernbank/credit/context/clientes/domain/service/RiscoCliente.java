package com.modernbank.credit.context.clientes.domain.service;

/**
 * Níveis de risco do cliente obtidos a partir de histórico (Grafo).
 */
public enum RiscoCliente {
    BAIXO,
    MEDIO,
    ALTO
}
